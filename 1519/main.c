#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char v[1000], abr[1000];

    fflush(stdin);
    while(gets(v)!='.')
    {
        for(i=0; i<strlen(v); i++)
        {

        }
    }
    printf("Hello world!\n");
    return 0;
}

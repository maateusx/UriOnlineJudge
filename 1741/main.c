#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char vet[1000001];
    int n;

    fflush(stdin);
    while(gets(vet)!=NULL)
    {
        for(i=0; i<)
    }
    printf("Hello world!\n");
    return 0;
}

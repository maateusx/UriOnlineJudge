#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, i, j, v[10];
    char c[100];
    scanf("%d", &n);

    while(n!=0)
    {
        gets(c);
        printf("%d", v);
        n--;
    }
    return 0;
}

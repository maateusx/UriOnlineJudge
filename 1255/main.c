#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int n, i, j, cont;
    char[200];
    scanf("%d ", &n);

    while(n!=0)
    {
        gets(v);
        for(i=0; i<strlen(v); i++)
        {
            for(j=0; j<strlen(v); j++)
            {
                if(j!=i && v[i]==v[j])
                {

                }
            }
        }
        n--;
    }
    return 0;
}

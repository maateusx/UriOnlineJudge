#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, i, vet[100];

    vet[0]=2;
    while(scanf("%d", &n)!=0)
    {
        if(n==0)
            break;
        for(i=1; i<100; i++)
        {
            for(j=0; j<100; j++)
            {

            }
        }
        printf("%d\n", vet[n]);
    }
    return 0;
}
